name = "call arity mismatch"
code = """
MODULE Main;
IMPORT MATH;
VAR
    x: REAL;
BEGIN
    x := MATH.FLT(1, 2);
END Main.
"""
result = { code = "E019", messages = ["Internal function 'MATH.FLT' does not accept argument types (INTEGER, INTEGER)", "FLT(INTEGER) -> REAL"] }
