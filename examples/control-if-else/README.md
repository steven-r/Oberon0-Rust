# Control Flow: IF / ELSE

This example demonstrates conditional branching:

- arithmetic condition expression
- `THEN` and `ELSE` branches
- branch result output

## Source

- `src/Main.ob0`

## Run

```bash
scripts/oberon0 examples/control-if-else --run
```

## Expected output

```text
0
State: {"x": 4}
```
