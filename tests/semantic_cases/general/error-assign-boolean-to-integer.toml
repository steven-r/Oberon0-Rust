name = "assign boolean to integer"
code = """
MODULE Main;
VAR flag: BOOLEAN;
VAR x: INTEGER;
BEGIN
    x := flag
END Main.
"""
result.code = "E012"
result.messages = ["cannot assign BOOLEAN to INTEGER 'x'"]
