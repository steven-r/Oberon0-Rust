name = "record field can be passed to a value parameter"
code = """
MODULE Main;
TYPE
  Person = RECORD
    age: INTEGER;
  END;
VAR p: Person;
PROCEDURE Bump(target: INTEGER);
BEGIN
  p.age := target + 1
END Bump;
BEGIN
  p.age := 1;
  Bump(p.age)
END Main.
"""
is_success = true
