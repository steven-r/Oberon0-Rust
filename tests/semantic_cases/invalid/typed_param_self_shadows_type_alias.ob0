(* INVALID: parameter reuses its type-alias name in own declaration (Count: Count) *)
MODULE Main;
IMPORT IO;
TYPE Count = INTEGER;
PROCEDURE P(Count: Count);
BEGIN
  IO.WriteInt(Count)
END P;
BEGIN
END Main.
