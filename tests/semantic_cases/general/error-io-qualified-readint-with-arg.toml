name = "io qualified readint with argument"
code = """
MODULE Main;
IMPORT IO;
VAR x: INTEGER;
BEGIN
    x := IO.ReadInt(1)
END Main.
"""
result.code = "E019"
result.messages = [
  "Internal function 'IO.ReadInt' does not accept argument types (INTEGER)",
  "ReadInt() -> INTEGER",
]
