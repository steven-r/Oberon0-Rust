code = """
  MODULE Main;
  IMPORT IO;
  BEGIN
    IO.WriteLn("Hello, World!")
  END Main.
"""
name = "WriteLn with args"
result.code = "E019"
result.messages = [
  "Internal function 'IO.WriteLn' does not accept argument types (STRING)",
  "WriteLn()",
]
