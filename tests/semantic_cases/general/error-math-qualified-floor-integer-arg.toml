name = "math qualified floor integer arg"
code = """
MODULE Main;
IMPORT MATH;
VAR i: INTEGER;
BEGIN
    i := MATH.FLOOR(i)
END Main.
"""
result.code = "E021"
result.messages = [
  "Internal function 'MATH.FLOOR' argument 1 has an incompatible type",
  "FLOOR(REAL | LONGREAL) -> INTEGER",
]
