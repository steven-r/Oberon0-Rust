# record-field-access

Demonstrates issue set #9-#11 with one focused program:

- `RECORD` type declaration (`#9`)
- field-selection designators in assignment and expressions (`#10`)
- type-checked field arithmetic and assignment (`#11`)

Run from repository root:

```bash
scripts/oberon0 examples/record-field-access --run
```

Expected output:

```text
42
```
