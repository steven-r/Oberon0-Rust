name = "parameter self-shadows type alias in declaration"
code = """
MODULE Main;
IMPORT IO;
TYPE Count = INTEGER;
PROCEDURE P(Count: Count);
BEGIN
    IO.WriteInt(Count)
END P;
BEGIN
END Main.
"""
result.code = "E004"
result.messages = ["Duplicate symbol declaration: 'Count'"]
