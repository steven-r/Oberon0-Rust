name = "parameter shadows global type alias"
code = """
MODULE Main;
IMPORT IO;
TYPE Count = INTEGER;
PROCEDURE P(Count: INTEGER);
BEGIN
    IO.WriteInt(Count)
END P;
BEGIN
END Main.
"""
is_success = true
