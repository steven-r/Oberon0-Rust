code = """
MODULE Main;
IMPORT IO;
VAR x: INTEGER;
BEGIN
    x := IO.WriteInt(1)
END Main.
"""
name = "non-function builtin in call expression"
result = { code = "E007", messages = [
  "Builtin 'IO.WriteInt' received an invalid argument",
  "must be used as a statement call",
] }
